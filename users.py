"""This module handles the user-, and team-related events.
It connects to the data_handler module if necessary and
handles the returned results.
Pass the processed data back to the main routing module.

"""

import data_handler
